# 钉钉 AI 日报推送系统 — 开发计划

## 一、项目概述

构建一套**全自动、零操作**的 AI 资讯每日推送系统。每天自动采集 AI 行业新闻与论文，生成一个精美的网页日报，并将链接推送到钉钉群。老板点开链接即可阅读，**无需安装任何软件、无需 VPN**。

### 核心约束

| 约束 | 影响 | 应对 |
|------|------|------|
| **老板无 VPN** | 所有新闻链接、日报网页必须国内直接访问 | 仅采集国内源；网页部署在国内可达的平台 |
| **公司用钉钉**（定制版） | 推送渠道为钉钉，非企业微信 | 使用钉钉群自定义机器人 Webhook |
| **零日常操作** | 上线后老板和你都不需要每天做任何事 | 全链路自动化，异常时才需介入 |

---

## 二、方案：网页日报 + 钉钉链接推送（推荐）

**为什么不直接在钉钉里推送全部内容？**

| 对比项 | 钉钉内纯文本/Markdown | 网页日报 + 钉钉推送链接 |
|--------|----------------------|------------------------|
| 阅读体验 | 一般，Markdown 格式受限 | 精美排版，手机/电脑自适应 |
| 信息容量 | 受限于钉钉消息长度 | 无限制，想放多少放多少 |
| 链接跳转 | 支持，但排版紧凑难点击 | 每条新闻独立卡片，点击方便 |
| 历史归档 | 聊天记录里翻找困难 | 自动归档，可查任意一天的日报 |
| 分享转发 | 仅限群内 | 网页链接可分享给任何人 |

> **最终方案：每天自动生成一个日报网页，通过钉钉群机器人推送当天的链接。**

### 整体架构

```
┌─────────────┐    ┌──────────────────┐    ┌──────────────────┐    ┌────────────────┐
│  定时触发器   │───▶│  新闻采集 & 清洗  │───▶│  LLM 摘要 & 去重  │───▶│  生成静态网页   │
│ (每天 8:00)  │    │  (国内源并行采集) │    │  (过滤+排序+摘要) │    │  (HTML 日报)   │
└─────────────┘    └──────────────────┘    └──────────────────┘    └───────┬────────┘
                                                                          │
                                                                   ┌──────▼───────┐
                                                                   │  部署到网站    │
                                                                   │ (Vercel/CF)  │
                                                                   └──────┬───────┘
                                                                          │
                                                                   ┌──────▼───────┐
                                                                   │  钉钉推送链接  │
                                                                   │ (群机器人)    │
                                                                   └──────────────┘
```

---

## 三、技术选型

| 模块 | 方案 | 说明 |
|------|------|------|
| **运行环境** | GitHub Actions | 免费、零运维、代码托管一体化 |
| **开发语言** | Python 3.11+ | 爬虫/API/LLM 库成熟 |
| **定时触发** | GitHub Actions cron | 每天 UTC 00:00（北京时间 08:00） |
| **新闻采集** | RSS + 轻量爬虫 | 仅国内源，无需 VPN |
| **论文采集** | arXiv API（国内可访问） | cs.AI / cs.CL / cs.LG |
| **内容摘要** | DeepSeek API / 通义千问 API | 国内大模型，低成本 |
| **去重排序** | SimHash + 热度评分 | 文本去重 + 公司多样性控制 |
| **网页生成** | Jinja2 模板 → 静态 HTML | 响应式设计，手机友好 |
| **网页托管** | Vercel / Cloudflare Pages / GitHub Pages | 免费，国内可访问（详见下文） |
| **消息推送** | 钉钉自定义机器人 Webhook | Markdown 卡片 + 链接 |

---

## 四、数据源规划（全部国内可访问，无需 VPN）

### 4.1 AI 资讯数据源

| 数据源 | 获取方式 | 国内可访问 | 优先级 |
|--------|----------|-----------|--------|
| **36氪 AI 频道** | RSS / 网页抓取 | ✅ | P0 |
| **机器之心 (Synced)** | RSS (`jiqizhixin.com/rss`) | ✅ | P0 |
| **量子位 (QbitAI)** | 网页抓取 (`quantumbit.top`) | ✅ | P0 |
| **IT之家 AI 频道** | RSS (`ithome.com/rss/`) | ✅ | P0 |
| **InfoQ 中文站 AI 板块** | RSS / 网页抓取 | ✅ | P1 |
| **新浪科技** | RSS / 网页抓取 | ✅ | P1 |
| **澎湃新闻·科技频道** | 网页抓取 | ✅ | P1 |
| **腾讯科技** | 网页抓取 | ✅ | P2 |
| **百度 AI 资讯** | 搜索 API | ✅ | P2 |

> 已移除所有需要 VPN 的海外源（TechCrunch、The Verge、Hacker News 等），确保推送的每条新闻链接老板都能直接打开。

### 4.2 AI 论文数据源

| 数据源 | 获取方式 | 国内可访问 | 说明 |
|--------|----------|-----------|------|
| **arXiv** | 官方 API | ✅ 可直接访问 | 全球最大预印本平台，国内可达 |
| **PaperWeekly** | 网页抓取 | ✅ | 国内 AI 论文解读社区 |
| **Semantic Scholar** | 官方 API（免费） | ✅ | 辅助获取引用量排序 |

> 已移除 HuggingFace Daily Papers（国内访问不稳定），用 PaperWeekly 替代。

---

## 五、核心模块设计

### 5.1 新闻采集模块 (`collector/`)

```python
class NewsCollector:
    def collect_all(self, date: str) -> list[RawArticle]:
        sources = [
            RSS36Kr(), RSSJiqizhixin(), RSSIThome(),
            RSSSinatech(), CrawlerQuantumbit(), CrawlerInfoQ()
        ]
        raw_articles = []
        for source in sources:
            raw_articles.extend(source.fetch(date))
        return raw_articles

@dataclass
class RawArticle:
    title: str
    summary: str
    url: str             # 必须为国内可直接访问的链接
    source: str
    publish_time: datetime
    keywords: list[str]
```

### 5.2 内容过滤 & 去重模块 (`filter/`)

```
原始文章 ──▶ 时间过滤（24h 内）
         ──▶ 关键词匹配（行业词 + 公司词，至少命中 1 个）
         ──▶ 质量过滤（排除：股评、个人博客、标题党、营销软文）
         ──▶ 链接可达性校验（HEAD 请求验证 URL 200，国内节点发起）
         ──▶ 文本相似度去重（SimHash，阈值 0.7）
         ──▶ 公司多样性去重（同一公司最多保留 2 条）
         ──▶ 热度排序（阅读量 + 评论数 + 关键词权重）
         ──▶ 截取 Top 10-15 条
```

**关键词配置：**

```python
INDUSTRY_KEYWORDS = ["AI应用", "大模型", "智能体", "Agent", "算力",
                     "AIGC", "多模态", "RAG", "具身智能", "AI芯片"]

COMPANY_KEYWORDS  = ["华为", "英伟达", "NVIDIA", "阿里", "腾讯",
                     "字节", "抖音", "钉钉", "支付宝", "百度",
                     "小米", "OpenAI", "谷歌", "Google", "Grok",
                     "xAI", "Meta", "微软", "Microsoft", "DeepSeek"]
```

### 5.3 LLM 摘要生成模块 (`summarizer/`)

```python
PROMPT_TEMPLATE = """
你是一位专业的AI行业编辑。请为以下新闻生成：
1. 一句话标题（20字以内，信息密度高，不要标题党）
2. 精简描述（50字以内，言简意赅说明核心信息）

原标题：{title}
原文摘要：{summary}

输出JSON格式：{{"title": "...", "desc": "..."}}
"""
```

### 5.4 网页生成模块 (`generator/`) ⭐ 新增

每天生成一个静态 HTML 页面，响应式设计，手机和电脑都好看。

**页面效果示意：**

```
┌─────────────────────────────────────────┐
│           🤖 AI 日报                     │
│           2026年3月19日 星期四             │
├─────────────────────────────────────────┤
│                                         │
│  📰 AI 资讯                              │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │ 华为发布盘古大模型5.0            │    │
│  │ 多模态能力全面升级，面向政企      │    │
│  │ 客户开放API...                  │    │
│  │                   来源：36氪 →   │    │
│  └─────────────────────────────────┘    │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │ 阿里通义千问开源Qwen3            │    │
│  │ 性能对标GPT-4o，代码和数学       │    │
│  │ 能力大幅提升...                  │    │
│  │                 来源：机器之心 →  │    │
│  └─────────────────────────────────┘    │
│  ...                                    │
│                                         │
│  📄 AI 论文精选                          │
│  ...                                    │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │     📅 查看往期日报               │    │
│  └─────────────────────────────────┘    │
│                                         │
└─────────────────────────────────────────┘
```

**技术实现：**

```python
from jinja2 import Template

def generate_daily_page(news_list, paper_list, date_str):
    """用 Jinja2 模板生成当天日报 HTML"""
    template = Template(open("templates/daily.html").read())
    html = template.render(
        date=date_str,
        news=news_list,
        papers=paper_list
    )
    # 输出路径：site/2026/03/19.html
    output_path = f"site/{date_str.replace('-', '/')}.html"
    write_file(output_path, html)
    # 同时更新 site/index.html（最新一期 + 往期归档列表）
    update_index(date_str)
    return output_path
```

### 5.5 钉钉推送模块 (`pusher/`)

```python
import requests
import hmac
import hashlib
import base64
import time
import urllib.parse

def push_to_dingtalk(webhook_url: str, secret: str, page_url: str, date_str: str, top3_titles: list[str]):
    """推送日报链接到钉钉群"""
    # 钉钉签名验证
    timestamp = str(round(time.time() * 1000))
    string_to_sign = f"{timestamp}\n{secret}"
    hmac_code = hmac.new(secret.encode(), string_to_sign.encode(), digestmod=hashlib.sha256).digest()
    sign = urllib.parse.quote_plus(base64.b64encode(hmac_code))

    url = f"{webhook_url}&timestamp={timestamp}&sign={sign}"

    # 使用 actionCard 消息类型，一个按钮跳转到日报网页
    payload = {
        "msgtype": "actionCard",
        "actionCard": {
            "title": f"🤖 AI 日报 | {date_str}",
            "text": (
                f"## AI 日报 | {date_str}\n\n"
                f"**今日要闻：**\n\n"
                f"- {top3_titles[0]}\n\n"
                f"- {top3_titles[1]}\n\n"
                f"- {top3_titles[2]}\n\n"
                f"...... 共 10+ 条资讯"
            ),
            "btnOrientation": "0",
            "singleTitle": "📖 查看完整日报",
            "singleURL": page_url
        }
    }
    resp = requests.post(url, json=payload)
    return resp.json()
```

**钉钉推送效果示意：**

```
┌─────────────────────────────────┐
│  🤖 AI 日报 | 2026-03-19        │
│                                 │
│  今日要闻：                      │
│  · 华为发布盘古大模型5.0...      │
│  · 阿里通义千问开源Qwen3...      │
│  · 英伟达发布新一代AI芯片...      │
│  ...... 共 10+ 条资讯            │
│                                 │
│  ┌───────────────────────────┐  │
│  │     📖 查看完整日报         │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

> 老板在钉钉里看到卡片，点击按钮直接跳转到日报网页，阅读体验好。

---

## 六、网页托管方案（国内可访问，无需 VPN）

| 方案 | 费用 | 国内访问速度 | 部署方式 | 推荐度 |
|------|------|------------|----------|--------|
| **Vercel** | 免费（Hobby 计划） | ⭐⭐⭐⭐ 较快 | Git 推送自动部署 | ⭐⭐⭐⭐⭐ **首选** |
| **Cloudflare Pages** | 免费 | ⭐⭐⭐⭐ 较快 | Git 推送自动部署 | ⭐⭐⭐⭐ |
| **GitHub Pages** | 免费 | ⭐⭐ 偶尔慢 | Git 推送自动部署 | ⭐⭐⭐ |
| **阿里云 OSS 静态站** | ~¥1/月 | ⭐⭐⭐⭐⭐ 极快 | CLI 上传 | ⭐⭐⭐⭐ |
| **Netlify** | 免费 | ⭐⭐⭐ 一般 | Git 推送自动部署 | ⭐⭐⭐ |

> **推荐 Vercel**：免费、国内访问稳定、Git 推送即自动部署、自带 HTTPS 和 CDN，零运维。如果访问速度不理想可随时切换到阿里云 OSS。

---

## 七、钉钉接入步骤

### 7.1 创建钉钉群自定义机器人（5 分钟）

1. 打开钉钉群（你和老板所在的群，或新建一个「AI 日报」群）
2. 群设置 → 智能群助手 → 添加机器人 → 自定义（通过 Webhook 接入）
3. 设置机器人名称（如「AI 日报助手」）
4. **安全设置**选择「加签」模式 → 记录 **Secret 密钥**
5. 复制 **Webhook 地址**（格式：`https://oapi.dingtalk.com/robot/send?access_token=xxx`）
6. 将 Webhook 地址和 Secret 配置为环境变量

### 7.2 关于「定制版钉钉」的兼容性

你们公司使用的是包装后的钉钉，需要确认以下两点：

| 确认项 | 验证方法 | 备选方案 |
|--------|----------|----------|
| 群机器人 Webhook 是否可用 | 尝试创建一个自定义机器人 | 若不可用，走钉钉开放平台应用内消息推送 |
| 外部链接是否可点击跳转 | 在群里发一个链接试试 | 若限制外链，可改为推送完整文本内容 |

> 大多数定制版钉钉保留了群机器人和外链能力。若有限制，方案会做相应降级调整。

### 7.3 钉钉消息类型对比

| 类型 | 说明 | 本项目使用 |
|------|------|-----------|
| text | 纯文本 | ❌ |
| markdown | 支持标题/加粗/链接/引用 | 备用方案 |
| link | 链接卡片（有标题+摘要+图片+链接） | 备用方案 |
| **actionCard** | 卡片+按钮（可跳转链接） | **✅ 首选** |

---

## 八、项目目录结构

```
ai-daily-push/
├── .github/
│   └── workflows/
│       └── daily-push.yml          # GitHub Actions 定时任务
├── src/
│   ├── collector/                   # 数据采集模块
│   │   ├── __init__.py
│   │   ├── base.py                 # 采集器基类
│   │   ├── rss_collector.py        # RSS 通用采集器
│   │   ├── jiqizhixin.py           # 机器之心
│   │   ├── kr36.py                 # 36氪
│   │   ├── ithome.py               # IT之家
│   │   ├── sina_tech.py            # 新浪科技
│   │   ├── arxiv_collector.py      # arXiv 论文采集
│   │   └── paper_weekly.py         # PaperWeekly 论文
│   ├── filter/                      # 过滤 & 去重模块
│   │   ├── __init__.py
│   │   ├── keyword_filter.py       # 关键词匹配
│   │   ├── quality_filter.py       # 质量过滤
│   │   ├── dedup.py                # 文本相似度去重
│   │   └── url_validator.py        # 链接可达性校验
│   ├── summarizer/                  # LLM 摘要模块
│   │   ├── __init__.py
│   │   └── llm_summarizer.py       # 调用 LLM API
│   ├── ranker/                      # 排序模块
│   │   ├── __init__.py
│   │   └── hot_ranker.py           # 热度评分 & 排序
│   ├── generator/                   # 网页生成模块 ⭐
│   │   ├── __init__.py
│   │   └── page_builder.py         # Jinja2 生成 HTML
│   ├── pusher/                      # 推送模块
│   │   ├── __init__.py
│   │   ├── dingtalk_bot.py         # 钉钉群机器人
│   │   └── formatter.py            # 消息格式化
│   ├── config.py                    # 配置（关键词、数据源等）
│   └── models.py                    # 数据模型
├── templates/
│   ├── daily.html                   # 日报页面模板
│   └── index.html                   # 首页（最新+往期归档）
├── site/                            # 生成的静态站点（自动部署）
│   ├── index.html
│   ├── 2026/03/19.html
│   └── ...
├── main.py                          # 主入口
├── requirements.txt
├── vercel.json                      # Vercel 部署配置
├── .env.example
└── README.md
```

---

## 九、每日自动执行流程

```
START (每天 08:00 自动触发，无需人工操作)
  │
  ├─ 1. 并行采集多源新闻 + 论文（全部国内可达源）
  │     ├─ 36氪 · 机器之心 · IT之家 · 新浪科技
  │     ├─ 量子位 · InfoQ · 澎湃科技
  │     └─ arXiv · PaperWeekly
  │
  ├─ 2. 过滤 Pipeline
  │     ├─ 时间过滤（24小时内）
  │     ├─ 关键词匹配（行业词 + 公司词）
  │     ├─ 质量过滤（排除股评/自媒体/标题党）
  │     ├─ URL 可达性验证（确保国内能打开）
  │     ├─ SimHash 相似度去重
  │     └─ 公司多样性控制
  │
  ├─ 3. 热度排序 → 取 Top 10+ 新闻 + Top 5 论文
  │
  ├─ 4. LLM 生成一句话标题 + 精简描述（≤ 3000 字符）
  │
  ├─ 5. 生成当天日报 HTML 页面 + 更新首页归档
  │
  ├─ 6. Git 推送 → Vercel 自动部署网页（约 30 秒上线）
  │
  ├─ 7. 钉钉群机器人推送 actionCard（含前 3 条要闻 + 「查看完整日报」按钮）
  │
  └─ 8. 日志记录 & 异常邮件告警
END
```

---

## 十、上线后的日常操作 ⭐ 重点回答

### 老板需要做什么？

| 事项 | 答案 |
|------|------|
| 需要安装软件吗？ | **不需要**，只用现有的钉钉 |
| 需要 VPN 吗？ | **不需要**，所有链接国内直达 |
| 每天需要做什么？ | **什么都不需要做**。钉钉里会收到卡片消息，想看就点开，不想看就忽略 |
| 需要额外付费吗？ | **不需要** |

### 你（开发者）需要做什么？

| 事项 | 频率 | 说明 |
|------|------|------|
| **日常运行** | 无需操作 | 全自动，每天 08:00 自行运行 |
| **监控告警** | 被动接收 | 异常时会收到告警通知（邮件/钉钉），无异常则无感知 |
| **数据源维护** | 约每月 1 次 | 若某个新闻源改版或下线，需调整爬虫（通常 30 分钟内修复） |
| **关键词更新** | 按需 | 若老板想新增/调整关注的公司或领域，改配置文件即可 |
| **LLM API 续费** | 约每月 1 次 | DeepSeek API 余额用完需充值（月均 ¥30-90） |

> **总结：上线后 99% 的时间你和老板都不需要做任何事。** 只有数据源改版或老板提新需求时才需要介入。

---

## 十一、开发排期

| 阶段 | 任务 | 工时 | 产出 |
|------|------|------|------|
| **P1 - 基础框架** | 项目初始化、数据模型、配置管理 | 0.5 天 | 可运行的项目骨架 |
| **P2 - 数据采集** | 实现 4+ 个新闻源采集器 + 论文采集 | 1.5 天 | 能稳定获取原始数据 |
| **P3 - 过滤去重** | 关键词过滤、质量过滤、SimHash 去重、URL 校验 | 1 天 | 高质量新闻列表 |
| **P4 - LLM 摘要** | 接入 DeepSeek/通义千问 API、Prompt 调优 | 0.5 天 | 自动生成标题和描述 |
| **P5 - 网页生成** | HTML 模板设计、响应式布局、往期归档 | 1 天 | 精美的日报网页 |
| **P6 - 钉钉推送** | Webhook 对接、actionCard 消息格式 | 0.5 天 | 钉钉群内推送卡片 |
| **P7 - 部署上线** | Vercel 部署、GitHub Actions 配置、Secrets 管理 | 0.5 天 | 全自动化运行 |
| **P8 - 测试优化** | 端到端测试、摘要质量调优、异常处理、监控 | 1 天 | 稳定可靠的系统 |
| **合计** | | **6.5 天** | |

---

## 十二、完整成本估算

### 一次性成本

| 项目 | 费用 | 说明 |
|------|------|------|
| 开发人力 | 6.5 个工作日 | 你的开发时间 |
| 服务器购买 | **¥0** | 无需购买任何服务器 |
| 软件授权 | **¥0** | 全部使用开源技术和免费服务 |

### 持续运行成本（每月）

| 项目 | 费用 | 说明 |
|------|------|------|
| GitHub Actions 运行 | **¥0** | 免费额度：2000 分钟/月，本项目每天约 5 分钟，远远用不完 |
| Vercel 网页托管 | **¥0** | Hobby 计划免费，静态站完全够用 |
| 钉钉群机器人 | **¥0** | 完全免费 |
| LLM API（摘要） | **约 ¥30-90** | 每天约 15-20 条新闻摘要，DeepSeek API 极低价 |
| 域名（可选） | ¥0 ~ ¥50/年 | 不买域名用 Vercel 默认域名也可以（xxx.vercel.app） |
| **月总成本** | **约 ¥30-90** | **唯一的持续支出就是 LLM API 费用** |

> 如果选择通义千问免费额度（每月有一定免费 Token），月成本可进一步降至接近 **¥0**。

---

## 十三、后续扩展方向

- [ ] **往期归档浏览**：首页显示日历视图，可点击查看任意一天的日报
- [ ] **搜索功能**：在日报站点中搜索历史新闻
- [ ] **个性化订阅**：支持多个钉钉群/人订阅不同主题关键词
- [ ] **热点预警**：检测到重大 AI 事件时实时推送（不等每日定时）
- [ ] **AI 周报汇总**：每周五生成一份本周 AI 行业总结
- [ ] **多渠道推送**：扩展到飞书、微信等平台
- [ ] **Web 管理后台**：可视化管理关键词、数据源、推送时间

---

## 十四、风险 & 应对

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| 数据源网站改版 | 部分采集失败 | 多源冗余（9 个源），单源故障不影响整体 |
| RSS 链接失效 | 部分源不可用 | 定期巡检 + 备用爬虫方案 |
| LLM API 不可用 | 无法生成摘要 | 降级为原始标题 + 截断摘要直接推送 |
| Vercel 国内访问波动 | 网页打开慢 | 备选切换到阿里云 OSS（¥1/月） |
| 钉钉 Webhook 限流 | 推送失败 | 每天仅推送 1 次，远低于限流阈值（20 条/分钟） |
| 定制版钉钉限制功能 | 机器人或外链不可用 | 降级为钉钉内直发 Markdown 全文 |
| GitHub Actions 海外节点访问国内源慢 | 采集超时 | 设置合理超时 + 重试；极端情况迁移到阿里云 FC |
| 新闻质量不稳定 | 推送低质内容 | 持续优化过滤规则 + LLM 质量评分 |

---

## 十五、快速启动 Checklist

开发前需要你准备以下信息：

- [ ] **创建钉钉群机器人**：获取 Webhook URL 和 Secret（参考第七章步骤）
- [ ] **确认定制版钉钉兼容性**：测试机器人和外链功能是否可用
- [ ] **注册 LLM API**：推荐 [DeepSeek 开放平台](https://platform.deepseek.com/) 或 [通义千问](https://dashscope.aliyun.com/)，获取 API Key
- [ ] **注册 Vercel**：用 GitHub 账号登录 [vercel.com](https://vercel.com) 即可
- [ ] **创建 GitHub 仓库**：存放项目代码，连接 Vercel 自动部署

> 以上准备工作约 30 分钟可完成，之后即可开始 P1 阶段开发。
