# 环境准备 — 操作手册

> 代码已经全部写好并部署完成。本文档记录初始搭建步骤，供后续参考。

---

## 前置准备（已完成）

### 第 1 步：安装 Git

1. 访问 **https://git-scm.com/download/win**
2. 下载 **64-bit Git for Windows Setup**
3. 双击安装，一路 Next → Install → Finish

验证：PowerShell 输入 `git --version`

### 第 2 步：注册 GitHub 账号

1. 访问 **https://github.com/signup**
2. 注册并验证邮箱

账号：`trytrytry2026`

### 第 3 步：注册 DeepSeek，获取 API Key

1. 访问 **https://platform.deepseek.com/**
2. 手机号注册 → 左侧 **API Keys** → 创建 Key
3. 左侧 **充值** → 先充 ¥10

---

## 部署步骤（已完成）

### 步骤 A：GitHub 仓库

仓库地址：`https://github.com/trytrytry2026/ai-daily-push`

### 步骤 B：推送代码

```powershell
cd "C:\Users\Laptop\Desktop\推送新闻"
git add .
git commit -m "提交信息"
git pull --rebase origin main
git push
```

### 步骤 C：GitHub Secrets 配置

在仓库 **Settings → Secrets and variables → Actions** 中已配置：

| Name | 说明 |
|------|------|
| `DEEPSEEK_API_KEY` | DeepSeek API Key |

> `BASE_PATH` 已在 GitHub Actions workflow 文件中直接设置为 `/ai-daily-push`。

### 步骤 D：GitHub Pages 部署

1. 仓库 **Settings → Pages → Source** 选择 **GitHub Actions**
2. 每次 Actions 运行后自动部署到：**https://trytrytry2026.github.io/ai-daily-push/**

### 步骤 E：手动触发

GitHub 仓库 → **Actions** → **AI Daily Report** → **Run workflow**

---

## 日常操作

| 角色 | 操作 | 频率 |
|------|------|------|
| **老板** | 打开 `https://trytrytry2026.github.io/ai-daily-push/` 看日报 | 想看就看 |
| **你** | 无需操作，系统每天 8 点自动运行 | 全自动 |
| **你（偶尔）** | DeepSeek 余额不足时充值 | 约每 1-3 个月 |
| **你（异常时）** | GitHub Actions 查看日志排错 | 极少发生 |

---

## 如何推送代码更新

修改代码后，在 PowerShell 中执行：

```powershell
cd "C:\Users\Laptop\Desktop\推送新闻"
git add .
git commit -m "描述你的修改"
git pull --rebase origin main
git push
```

> 如果 `git push` 失败，先执行 `git pull --rebase origin main` 再 push。
> 因为 GitHub Actions 每天也会推送文件，可能存在远程更新需要先同步。

---

## 每月成本

| 项目 | 费用 |
|------|------|
| GitHub Actions + GitHub Pages + Edge TTS | ¥0 |
| DeepSeek API | 约 ¥10-30 |
| **合计** | **约 ¥10-30/月** |
