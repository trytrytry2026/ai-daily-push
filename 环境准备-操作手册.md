# 环境准备 — 操作手册（纯网页方案）

> 代码已经全部写好，你只需要完成以下 4 步注册 & 安装，然后按「上线部署」操作即可。
> 全部完成后系统每天自动运行，你和老板什么都不用做。

---

## 第 1 步：安装 Git（约 2 分钟）

1. 打开浏览器，访问 **https://git-scm.com/download/win**
2. 下载 **64-bit Git for Windows Setup**
3. 双击安装包，**一路 Next → Install → Finish**（全部默认即可）

**验证：** 重新打开 PowerShell，输入 `git --version`，看到版本号就行。

---

## 第 2 步：注册 GitHub 账号（约 3 分钟）

1. 访问 **https://github.com/signup**
2. 输入邮箱 → 设置密码 → 设置用户名（英文）
3. 通过人机验证 → 去邮箱点击验证链接

**验证：** 能登录 https://github.com 就 OK。

---

## 第 3 步：注册 DeepSeek，获取 API Key（约 2 分钟）

1. 访问 **https://platform.deepseek.com/**
2. 手机号注册登录
3. 左侧菜单 → **API Keys** → **创建 API Key** → 命名 `ai-daily`
4. **立刻复制 Key**（只显示一次！）
5. 左侧菜单 → **充值** → 先充 **¥10**

**验证：** API Keys 页面能看到你创建的 Key。

记下来：
```
DeepSeek API Key: sk-xxxxxxxxxxxxxxxx
```

---

## 第 4 步：注册 Vercel（约 1 分钟）

> 必须先完成第 2 步。

1. 访问 **https://vercel.com/signup**
2. 选择 **Continue with GitHub**
3. 点击 **Authorize Vercel** 授权

**验证：** 能看到 Vercel Dashboard 页面。

---

## 上线部署（代码已写好，按顺序执行即可）

### 步骤 A：在 GitHub 创建仓库

1. 登录 GitHub → 点右上角 **「+」** → **New repository**
2. 仓库名填：`ai-daily-push`
3. 选择 **Public**（Vercel 免费版需要 Public 仓库）
4. **不要**勾选 "Add a README file"
5. 点 **Create repository**
6. 你会看到一个页面显示推送指令，先不管

### 步骤 B：推送代码到 GitHub

打开 PowerShell，**逐行**执行以下命令（替换 `你的用户名`）：

```powershell
cd "C:\Users\Laptop\Desktop\推送新闻"
git init
git add .
git commit -m "初始化 AI 日报系统"
git branch -M main
git remote add origin https://github.com/你的用户名/ai-daily-push.git
git push -u origin main
```

> 首次推送会弹出 GitHub 登录窗口，登录即可。

### 步骤 C：配置 GitHub Secrets（保存 API Key）

1. 打开你的 GitHub 仓库页面 → **Settings** → **Secrets and variables** → **Actions**
2. 点 **New repository secret**
3. 添加以下两个 Secret：

| Name | Value |
|------|-------|
| `DEEPSEEK_API_KEY` | 你的 DeepSeek API Key（`sk-xxx`） |
| `SITE_BASE_URL` | 先留空，等步骤 D 拿到域名后再填 |

### 步骤 D：连接 Vercel 自动部署

1. 登录 **https://vercel.com/dashboard**
2. 点 **Add New...** → **Project**
3. 找到 `ai-daily-push` 仓库 → 点 **Import**
4. **Framework Preset** 选 **Other**
5. **Output Directory** 填 `site`
6. 点 **Deploy**
7. 部署完成后，你会得到一个网址如 `ai-daily-push.vercel.app`

> 这个网址就是你老板每天打开的页面！把它发给老板收藏即可。

### 步骤 E：手动触发第一次运行

1. 打开 GitHub 仓库 → **Actions** 标签页
2. 左侧选 **AI Daily Report**
3. 点 **Run workflow** → **Run workflow**
4. 等待约 2-3 分钟执行完成（绿色勾 = 成功）
5. 打开你的 Vercel 网址，应该能看到今天的日报了！

---

## 上线后日常操作

| 角色 | 需要做什么 | 频率 |
|------|-----------|------|
| **老板** | 什么都不用做，打开收藏的网址就能看最新日报 | 随时想看就看 |
| **你** | 什么都不用做，系统每天 8 点自动运行 | 全自动 |
| **你（维护）** | 如果某天日报没更新，去 GitHub Actions 看日志排错 | 极少发生 |

---

## 每月成本

| 项目 | 费用 |
|------|------|
| GitHub（代码 + 自动任务） | ¥0 |
| Vercel（网页托管） | ¥0 |
| DeepSeek API（新闻摘要） | 约 ¥30-90 |
| **合计** | **约 ¥30-90/月** |
